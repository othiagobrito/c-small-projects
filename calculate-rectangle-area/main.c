#include <stdio.h>

int main()
{
    const int length = 15;
    const int width  = 30;
    const int area = length + width;

    printf("Length: %d\n", length);
    printf("Width: %d\n", width);
    printf("Area: %d", area);

    return 0;
}
